{% extends 'base.html.twig' %}

{% block title %}
    Créer une sortie | {{ parent() }}
{% endblock %}

{% block body %}
<div class="container">
    <div class="col align-self-center">
    <h1>Formulaire de création de sortie</h1>

    {{ form_start(sortie_form, {attr: {'novalidate': 'novalidate'}}) }}

    {{ form_row(sortie_form.nom) }}
    {{ form_row(sortie_form.dateDebut) }}
    {{ form_row(sortie_form.dateClotureInscription) }}
    {{ form_row(sortie_form.nombreInscriptionMax) }}
    {{ form_row(sortie_form.duree) }}
    {{ form_row(sortie_form.descriptionInfos) }}
{#    {{ form_row(sortie_form.campus) }}#}
{#    {{ form_row(sortie_form.ville)}}#}
    {{ form_row(sortie_form.lieux) }}
{#    {{ form_row(sortie_form.rue) }}#}
{#    {{ form_row(sortie_form.codePostal) }}#}
{#    {{ form_row(sortie_form.latitude) }}#}
{#    {{ form_row(sortie_form.longitude) }}#}


        <button type="submit" name="enregistrer" value="creer" class="btn btn-lg btn-success">Enregistrer la sortie</button>

        <button type="submit" name="enregistrer" value="publier" class="btn btn-lg btn-success">Publier la Sortie</button>

        <button type="submit" name="enregistrer" value="annuler" class="btn btn-lg btn-success">Annuler la Sortie</button>

    {{ form_end(sortie_form) }}
        </div>
    </div>
{% endblock %}
